# Knot.ai

Landing page for Knot.ai — inference for the agentic era: open models on Indian
infrastructure, built for workloads that run for hours, not seconds.

Single static page. `index.html` carries its own CSS and JS inline; assets sit
alongside it. No build step — serve the folder.
